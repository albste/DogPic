// Model for Dog Sub Breeds
class DogSubBreedModel {
  final int id;
  final int parentBreedId;
  final String title;

  DogSubBreedModel({
    required this.id,
    required this.parentBreedId,
    required this.title,
  });
}
